import { View, Text } from "react-native";
import React from "react";

const Dot = () => {
  return (
    <View
      style={{
        height: 5,
        width: 5,
        backgroundColor: "#FFFFFF",
        borderRadius: 100,
        margin: 8,
      }}
    ></View>
  );
};

export default Dot;
